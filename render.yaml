services:
  - type: web
    name: party-queue-backend
    runtime: node
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    envVars:
      - key: PORT
        value: 4000
      - key: YOUTUBE_API_KEY
        sync: false
